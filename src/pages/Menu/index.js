import React from 'react';
import ListMenu from '~/components/ListMenu';

const MenuPage = () => {
    return (
        <div>
            <ListMenu></ListMenu>
        </div>
    );
};

export default MenuPage;
